export function makeStatsString(characterCount, environmentCount, opponentCount) {
    return `You have clicked the character selector ${characterCount} times, the environment selector ${environmentCount} times, and the opponent selector ${opponentCount} times. Your champion fought hard!`;
}

